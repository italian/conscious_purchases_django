meta:
  version: "3.8"
  environment: python
  toolchain: pip
build:
  requirementsPath: requirements.txt
run:
  persistenceMount: /data
  containerPort: "8080"
  scriptName: python manage.py runserver
